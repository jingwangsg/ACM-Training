{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build with g++",//这个任务的名字在launch.json最后一项配置
            "type": "shell",
            "command": "clang++",
            "args": [
                "-std=c++17",
                //"test.cpp",这里是官方写法，不具有普遍性，注意两个配置文件的统一性即可
                "${fileDirname}/${fileBasenameNoExtension}.cpp",
                "-o",
                "${fileDirname}/${fileBasenameNoExtension}.o",
                "-g"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}