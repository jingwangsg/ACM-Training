#include<iostream>
using namespace std;

int n, T;

int main() {
    #ifndef ONELINE_JUDGE
    freopen("in.txt", "r", stdin);
    #endif
    ios::sync_with_stdio(false);

    cin >> T;

    while (T--) {

    }
    return 0;
}